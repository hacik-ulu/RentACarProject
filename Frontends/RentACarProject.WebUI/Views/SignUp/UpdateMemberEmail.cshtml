@model UpdateMemberEmailDto
@{
    ViewData["Title"] = "UpdateMemberEmail";
    Layout = "~/Views/UILayout/Index.cshtml";
}

<div class="page-content">
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-lg-11">
                <div class="card shadow-sm border-0" style="margin-top: 30px; margin-bottom: 30px;">
                    <div class="card-header bg-primary text-white">
                        <h4 class="card-title mb-0" style="color: black;">Edit Email</h4>
                        <p class="card-subtitle mt-1">Update your Email below.</p>
                    </div>
                    <div class="card-body bg-light" style="color: black;">
                        <form method="post" asp-action="UpdateMemberEmail" asp-controller="SignUp">
                            <input type="hidden" asp-for="AppUserID" />
                            <div class="mb-3">
                                <label for="Username" class="form-label"><strong>Email</strong></label>
                                <input type="text" asp-for="Email" class="form-control" id="Email">
                                <span asp-validation-for="Email" class="text-danger"></span>
                            </div>
                            <div class="d-grid gap-2 d-md-block">
                                <button type="submit" class="btn btn-primary me-3">Save</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

